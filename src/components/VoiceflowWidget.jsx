import React, { useEffect } from 'react';

function VoiceflowWidget() {
  useEffect(() => {
    // The widget is initialized via the script in index.html
    // This component exists to provide a React wrapper and potential future functionality
  }, []);

  return null; // This component doesn't render anything visible
}

export default VoiceflowWidget;